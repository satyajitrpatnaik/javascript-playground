import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  root: {
    margin: "20px",
    height: "50vh",
    width: "70vw",
  },
  title: {
    color: "#fff",
    marginBottom: "20px",
  },
  card: {
    padding: "20px",
  },
}));
